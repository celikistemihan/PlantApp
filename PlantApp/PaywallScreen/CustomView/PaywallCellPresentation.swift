//
//  PaywallCellPresentation.swift
//  PlantApp
//

import Foundation

struct PaywallCellPresentation {
    let primaryText: String
    let subText: String
    let image: String
}

let cells: [PaywallCellPresentation] = [
    PaywallCellPresentation(primaryText: "Unlimited", subText: "Plant Identify", image: "unlimited"),
    PaywallCellPresentation(primaryText: "Faster", subText: "Process", image: "faster"),
    PaywallCellPresentation(primaryText: "Unlimited", subText: "Plant Identify", image: "unlimited")
]
